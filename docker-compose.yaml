services:
  frost-http:
    image: fraunhoferiosb/frost-server:latest
    environment:
      # For all settings see: https://fraunhoferiosb.github.io/FROST-Server/settings/settings.html
      - serviceRootUrl=http://localhost:8080/FROST-Server
      - plugins_multiDatastream.enable=false
      - http_cors_enable=true
      - http_cors_allowed_origins=*
      - persistence_db_driver=org.postgresql.Driver
      - persistence_db_url=jdbc:postgresql://database:5432/sensorthings
      - persistence_db_username=sensorthings
      - persistence_db_password=ChangeMe
      - persistence_autoUpdateDatabase=true
      - persistence_idGenerationMode=ServerAndClientGenerated
    ports:
      - 8080:8080
      - 1883:1883
    depends_on:
      database:
        condition: service_healthy

  database:
    container_name: database
    build:
      context: ./docker
    environment:
      - POSTGRES_DB=sensorthings
      - POSTGRES_USER=sensorthings
      - POSTGRES_PASSWORD=ChangeMe
    volumes:
      - postgis_volume:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d sensorthings -U sensorthings "]
      interval: 10s
      timeout: 5s
      retries: 5

  pygeoapi:
    image: geopython/pygeoapi:latest
    volumes:
      - ./config.yml:/pygeoapi/local.config.yml
    depends_on:
      - frost-http
    ports:
      - 5000:80

  ui:
    image: internetofwater/hub2box-ui:iow
    depends_on:
      - pygeoapi
    ports:
      - 5001:80
    environment:
      - WIS2BOX_API_BACKEND_URL=http://localhost:5000/
      - WIS2BOX_URL=http://localhost:5001
      - serviceRootUrl=http://localhost:8080/FROST-Server
      - WIS2BOX_API_TYPE=pygeoapi
      - WIS2BOX_API_BACKEND_TYPE=SensorThings
      - WIS2BOX_API_BACKEND_URL=http://localhost:8080/FROST-Server/v1.1

volumes:
    postgis_volume:

